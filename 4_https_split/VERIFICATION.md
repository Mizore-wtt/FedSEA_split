# 第四阶段验证记录

[启动说明](README.md) | [项目入口](../README.md) | [协议与安全边界](../docs/https.md)

验证日期：**2026-09-08，北京时间**。本页是本机实验记录，不是模型质量、速度或隐私保证。

## 最终结论

Qwen2.5-0.5B-Instruct 已完成同机两个进程的 HTTPS 三段推理。
正常客户端只加载 A/C、Embedding、Norm、LM Head；服务端只加载 B。
两端各自保存所属层的 KV Cache。只有对比模式额外加载完整参考模型。
没有加噪、下载大模型、修改共用模型/推理/切分配置，或改写前三阶段代码。

| 切分 | 最终对比记录 | 状态 | 张量检查 | 最大绝对误差 |
|---|---|---|---|---|
| 2+20+2 | [20260908T011759Z_0bc78c79](runs/20260908T011759Z_0bc78c79/run.json) | passed | 398 / 398，逐元素相等 | 0 |
| 1+20+3 | [20260908T011538Z_18b04067](runs/20260908T011538Z_18b04067/run.json) | passed | 398 / 398，逐元素相等 | 0 |

两轮均检查 5 组固定问题、4 组输入长度和 3 轮会话操作。
每轮共比较 142 个生成步（含 EOS），生成 token 序列和解码文字一致，
所有检查张量有限。配置仍为 `atol=1e-5, rtol=1e-5`，没有放宽容差。
最终测试值实际达到精确相等，而不只是容差内通过。

输入长度检查为 `1 / 128 / 512 / 1024` token，每组再检查 3 个增量 decode 步。
检查 A 发出、B 返回、C 归一化后的边界张量、logits 和本地 A/C KV。
B 的 KV 不通过业务接口导出；真实 HTTPS 测试检查 B 的边界输出和缓存长度，
随机小模型测试则直接比较全部 A/B/C 层的 K/V。

多轮用例先记住 17，再追问数字，第二轮复用 42 个 token；重置后计算 3+5，
复用数回到 0。每轮完成时均符合缓存长度等于输出序列长度减一的约定。

## 确实是两个进程

| 运行 | 客户端 PID | 服务端实际 Python PID | 结束后的服务端会话数 |
|---|---|---|---|
| 2+20+2 最终轮 | 15664 | 32632 | 0 |
| 1+20+3 最终轮 | 3768 | 10996 | 0 |

Windows 虚拟环境可能还有一个 Python 转发进程；表中使用处理请求的实际 PID。
自动验证用随机实例标识核对自己的服务端，并在退出时关闭自己创建的进程树，
不停止其他占用端口的程序。启动/关闭记录：

- [默认切分启动记录](runs/launcher_20260908T011747Z_b3836bcd/launcher.json)
- [另一种切分启动记录](runs/launcher_20260908T011526Z_6f92c69c/launcher.json)

两个切分的 p+q 都是 4，所属参数总量相同：

| 角色 | 参数量 | FP16 参数字节数 |
|---|---|---|
| 客户端 A/C 及词嵌入/输出头等 | 195,785,088 | 391,570,176 |
| 服务端 B | 298,247,680 | 596,495,360 |
| 合计 | 494,032,768 | 988,065,536 |

这些是参数大小，不是进程总显存。缓存、激活、CUDA 上下文和测试参考模型另占空间。
模型仍只在共用 models 目录存一份 checkpoint，没有在阶段目录复制权重。

## 普通聊天启动检查

[聊天检查记录](runs/smoke_20260908T011947Z_d646fb65/smoke.json) 和同目录 `client.log` /
`server.log` 使用预设的非敏感输入，单独检查普通 `--chat` 路径，不加载完整参考模型。

- 第一轮回答“你好。”，下一轮正确续答“你好”，复用 33 个 token。
- `/reset` 后计算 3+5，回答 8，复用数为 0。
- `/exit` 正常退出，远程会话数为 0，测试服务端已停止。

该检查脚本显式保存测试日志；正常交互客户端自身仍不保存聊天记录。

## 自动测试

本次在实际工作空间运行，共 **137 项通过**：

| 范围 | 数量 |
|---|---|
| 共用模型、切分与命令行接口 | 26 |
| 第一阶段 | 10 |
| 第二阶段 | 35 |
| 第三阶段 | 31 |
| 第四阶段 | 35 |

第四阶段覆盖：

- Qwen2 / Qwen3-MoE，小型随机权重；tied/untied embedding-head；单文件/分片 safetensors。
- 2+2+2、1+2+3、3+2+1 小模型切分；所属参数完整且互不重复；全部 A/B/C KV 比较。
- 预填充、单 token 增量、左侧 padding、多轮复用、改前缀、裁剪、重置及会话隔离。
- B 部分计算失败、C 部分失败、B 已提交但响应丢失；不重试前向，清除不确定缓存。
- 会话容量、TTL、模型身份不一致、乱序/重放、缓存位置、mask 和上下文校验。
- 真实临时 HTTPS；localhost / 127.0.0.1 的 SAN；不可信 CA、错误主机名、错误令牌被拒绝。
- HTTP 明文、异常帧、重复 JSON 键、超大消息、异常张量维度和非有限隐藏状态被拒绝。
- 配置预览、Qwen3 预留接口和不落盘的命令行覆盖。
- CUDA 长位置编码初始化回归；本机 CUDA 可用，该项实际执行通过，不是跳过。

第四阶段还以 `-W error::ResourceWarning` 运行，连接与文件资源检查通过。
测试不是独立安全审计，也未穷举所有资源耗尽、断网和并发竞争情形。

## 环境与证书

模型固定为 `Qwen/Qwen2.5-0.5B-Instruct`：

```text
revision:
7ae557604adf67be50417f59c2c2f167def9a775
model.safetensors SHA-256:
fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe
```

Python 3.11.15；torch 2.8.0+cu126；Transformers 4.56.1；
NVIDIA RTX A3000 Laptop GPU；FP16，eager attention，贪心生成。
运行记录包含实际环境、角色参数统计、模型文件指纹和源码 SHA-256。
推理全部使用已有本地权重，未新增 pip 依赖，证书工具使用本机 OpenSSL。

`certs/localhost/` 已准备完成，当前不需要再次生成。
服务端证书的有效期为 **2026-09-08 08:59:50 至 2026-12-07 08:59:50，北京时间**，
SAN 为 localhost 和 127.0.0.1。使用项目内 CA，不修改 Windows 全局信任。
目录限制为当前用户，私钥/令牌不进入版本控制，也未写入本页或运行报告。

## 排错历史

失败记录保留，没有删除或改成通过：

- 最早启动检查将 Windows venv 转发进程的 PID 当成工作进程 PID，因此主动退出。
  已改为随机启动实例标识识别，并清理自己创建的进程树。
- [20260908T010630Z_1a88cef9](runs/20260908T010630Z_1a88cef9/run.json)
  中长输入出现误差。定位到直接在 GPU 初始化 RoPE 频率与原生 CPU 初始化后迁移
  的路径相差约 `1.86e-9`，在较长位置经 FP16 舍入后产生可测差异。
  已匹配原生 CPU 初始化再迁移的路径，保持 FP32 频率，并增加 CUDA 回归测试。
- 此后的 [首次修复验证](runs/20260908T011157Z_70f8c5a3/run.json) 及表中的两轮最终验证通过。

## 尚未实现

本机 loopback 原型，不是公网服务或跨机器部署。真实 30B 权重未下载/运行，
仅预留配置和验证小型 MoE 结构。未实现噪声、差分隐私保证、训练/联邦聚合、
采样、量化、卸载、多 GPU、动态自适应切分或跨故障缓存恢复。
HTTPS 服务端仍可看到隐藏张量，不能把“不传文字/token ID”当作隐私证明。
对比中启用记录和数值检查，通信字节只含应用消息体，不含 HTTP/TLS/TCP；
不依据这些耗时宣称加速。
