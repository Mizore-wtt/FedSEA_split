# 文档与模型接口优化验证

[返回项目入口](../README.md)

验证日期：2026-09-07。实际目录：`E:\0_wt_ws\FedSEA`。
本页是第三阶段之前的历史记录，当前缓存进展见
[第三阶段验证](../3_kv_cache/VERIFICATION.md)，以下原始结果保留。
本记录针对本次文档整理、共用加载器、模型清单和切分预设调整，
不替代两个阶段此前的历史 `VERIFICATION.md`。

## 本次改动

- 根 README 提供启动和导航；各阶段说明自己的操作；共用细节放在 `docs/`。
- `configs/model.json` 管模型选择；`inference.json` 管推理；`splits.json` 管组合。
- 两阶段都使用 `fedsea/` 共用加载器，移除旧的跨阶段 `stage1_bridge.py`。
- 模型与切分可独立选择，k 自动计算；命令行与阶段配置优先级有测试。
- Qwen3-MoE 适配器复用原有层、专家、路由和输出头，不创建第二份权重。
- 模型准备支持 safetensors 分片索引和单独的聊天模板文件。
- 历史检查区分模型身份、模板设置和缺失数组，不把跳过说成通过。
- 保留了工作期间新增的中文学习注释；共用函数的注释随函数移动。

## 单元与接口测试

在实际项目环境运行以下三个测试目录，合计 **71 项通过**：

| 测试目录 | 数量 | 范围 |
|---|---|---|
| `tests/` | 26 | 模型清单、切分优先级、分片、模板文件、CLI 预览与禁用保护 |
| `1_model_base/tests/` | 10 | 基线配置、提示词和长度校验 |
| `2_model_sep/tests/` | 35 | Qwen2 拆分、MoE 适配器、边界比较、历史记录兼容 |

MoE 其中 6 项使用小型随机权重，检查多种切分前向、多步生成、padding、
专家/输出头复用以及错误适配器和不支持输出的拒绝行为。
**这不等于真实 Qwen3-30B checkpoint 验证通过。**

`--describe --model qwen3-30b-a3b-instruct-2507 --split p1_q3`
预览得到 `1+44+3`，不加载权重。两个预留模型均未启用，
实际推理或准备命令会在访问权重/下载前拒绝。

## 真实小模型回归

环境保持 Python 3.11.15、PyTorch 2.8.0+cu126、Transformers 4.56.1，
RTX A3000 Laptop GPU 6GB、FP16、eager、贪心生成。

第一阶段新的 `--benchmark --save-artifacts` 全部通过。
3 个问题的输入 token、输出 token、首 token logits 共 9 个数组，
与原基线 `20260907T023904Z_d49a4ec1` 逐项完全一致。

| 第二阶段测试 | 默认 2+20+2 | 预设 p1_q3：1+20+3 |
|---|---|---|
| 5 个问题与累计 133 步生成 | 通过 | 通过 |
| 每步原始 logits、token、回答 | 完全相等 | 完全相等 |
| A/B/C 边界、输入 logits | 完全相等 | 完全相等 |
| 1/128/512/1024 token 合成输入 | 通过 | 通过 |
| 每组 169 项张量比较 | 全部 exact，最大误差 0 | 全部 exact，最大误差 0 |
| 原有 3 个问题的历史基线检查 | passed | passed |
| 新增 2 个问题的历史状态 | not_applicable | not_applicable |

本次运行目录均保存在所属阶段，不覆盖旧结果：

```text
1_model_base/runs/20260907T065445Z_8ac2e6b9/
2_model_sep/runs/20260907T065534Z_001eea84/
2_model_sep/runs/20260907T065654Z_295c6255/
```

## 交互与完整性

从项目外用完整路径启动第一阶段聊天，生成、`/reset`、`/exit` 正常。
第二阶段用 `run.ps1 --chat --p 1 --q 3` 启动，打印 `1+20+3`；
多轮测试先记住 APPLE，下一轮正确返回 APPLE，重置和退出正常。
聊天均未保存会话，测试进程均已结束。

小模型权重文件复核 SHA-256，仍为：

```text
fdf756fa7fcbe7404d5c60e26bff1a0c8b8aa1f72ced49e7dd0210fe288fb7fe
```

未安装新依赖，未下载 30B 权重，未修改小模型文件或历史运行数组。
Qwen3 后续仍需规划放置与硬件、固定 revision、部署真实权重并独立回归。
分段缓存、HTTPS、噪声、量化和多设备运行仍未实现；本次不做加速或隐私保证结论。
